import os
import telebot

BOT_TOKEN = os.environ.get("BOT_TOKEN")

bot = telebot.TeleBot(BOT_TOKEN)

@bot.message_handler(commands=["start"])
def start(message):
    bot.reply_to(
        message,
        "🎉 Assalomu alaykum!\n\n"
        "Moxigul Konkurs botiga xush kelibsiz! ❤️"
    )

bot.infinity_polling()
